# Dataverse / Microsoft Dynamics 365 Configuration
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# Azure Active Directory Tenant ID
# Find this in Azure Portal > Azure Active Directory > Overview
TENANT_ID=your-tenant-id-here

# Azure AD Application (Client) ID
# Find this in Azure Portal > App Registrations > Your App > Overview
CLIENT_ID=your-client-id-here

# Azure AD Application Client Secret
# Find this in Azure Portal > App Registrations > Your App > Certificates & secrets
CLIENT_SECRET=your-client-secret-here

# Dataverse Environment URL (Resource)
# Example: https://orgXXXXXX.api.crm8.dynamics.com
RESOURCE=https://your-org.api.crmX.dynamics.com

# Dataverse Base URL (same as RESOURCE)
BASE_URL=https://your-org.api.crmX.dynamics.com

# Dataverse API Version (optional, defaults to /api/data/v9.2)
DATAVERSE_API=/api/data/v9.2

# Server Port (optional, defaults to 3001)
PORT=3001

# ============================================
# How to get your Dataverse credentials:
# ============================================
#
# 1. Go to Azure Portal (portal.azure.com)
# 2. Navigate to "App Registrations"
# 3. Create a new app or select existing one
# 4. Copy the following:
#    - Application (client) ID → CLIENT_ID
#    - Directory (tenant) ID → TENANT_ID
# 5. Go to "Certificates & secrets"
#    - Create a new client secret
#    - Copy the secret VALUE → CLIENT_SECRET
# 6. Go to Power Platform Admin Center (admin.powerplatform.microsoft.com)
#    - Select your environment
#    - Copy the Environment URL → RESOURCE and BASE_URL
# 7. Grant API permissions in Azure AD app:
#    - Dynamics CRM > user_impersonation
#
# ============================================
# Security Best Practices:
# ============================================
#
# ✅ DO:
# - Keep .env file in .gitignore
# - Use different secrets for dev/staging/production
# - Rotate secrets regularly
# - Use Azure Key Vault for production
#
# ❌ DON'T:
# - Commit .env to git
# - Share secrets in chat/email
# - Use same secrets across environments
# - Hardcode secrets in source code
#
